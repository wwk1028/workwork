# 机器翻译系统开题报告
## 一、问题描述与分析
机器翻译是自然语言处理领域的核心任务，旨在实现不同语言间的自动转换，可有效降低人工翻译成本、提升跨语言信息传递效率，广泛应用于文档翻译、跨境交流、内容创作等场景。

本项目聚焦中文与英文的互译任务，计划从头搭建并训练机器翻译模型，实现中文到英文的基础翻译功能，并扩展英文到中文的双向翻译能力，完成从模型训练到服务部署、界面交互的完整工程实践流程。

## 二、功能需求
1. 基础功能：输入中文文本，输出对应的英文翻译结果；
2. 扩展功能：输入英文文本，输出对应的中文翻译结果（加分项）；
3. 服务部署：模型封装为 Web API，支持跨接口调用；
4. 交互界面：基于 Gradio 实现可视化界面，包含文本输入框、翻译按钮、结果展示区；
5. 模型能力：支持自定义数据训练、模型保存与加载、翻译结果实时返回。

## 三、方案选择与设计
### （一）模型选择
- 基础模型：采用 **Seq2Seq + BiLSTM + 注意力机制**，结构简洁、易实现，适合课程实践场景；
- 扩展模型：可选 Transformer 模型，提升长文本翻译精度（加分项）。

### （二）技术选型
- 深度学习框架：选用 PyTorch，动态图机制灵活、社区生态完善；
- 词嵌入：采用随机初始化或 Word2Vec 预训练嵌入；
- 解码策略：基础采用贪心搜索，扩展支持 Beam Search；
- 后端服务：选用 FastAPI，高性能、易部署、支持异步请求；
- 前端界面：选用 Gradio，快速搭建交互界面、无需前端开发经验。

### （三）数据集选择
选用 **AI Challenger 2017 中英平行语料**，数据规模充足、覆盖场景广泛，开源易获取，满足从头训练模型的需求。

## 四、软件结构设计
### （一）模块划分
1. 数据模块：负责原始数据读取、文本分词、词汇表构建、数据集划分与批量生成；
2. 模型模块：实现 Encoder、Decoder、Seq2Seq 网络搭建，包含模型训练、验证、保存与加载功能；
3. 推理模块：封装模型推理逻辑，接收文本输入、返回翻译结果；
4. 后端模块：基于 FastAPI 开发翻译接口，实现请求接收、参数校验、结果返回；
5. 前端模块：基于 Gradio 设计交互界面，对接后端 API，实现用户输入与结果展示。

### （二）模块输入输出
1. 数据模块：输入原始中英文本 → 输出分词后的 token 序列、词汇表、批量数据；
2. 模型模块：输入批量训练数据 → 输出训练损失、验证精度、预测翻译序列；
3. 后端接口：输入 JSON 格式文本与语言类型 → 输出 JSON 格式翻译结果；
4. 前端界面：输入用户文本 → 显示后端返回的翻译结果。

## 五、进度计划
1. 第12周：完成项目选题、开题报告撰写与提交；
2. 第13周：完成数据集下载、清洗、预处理，搭建基础模型框架；
3. 第14周：完成模型训练、参数调优，提交中期报告；
4. 第15-16周：完成 FastAPI 接口开发、模型部署、Gradio 界面设计；
5. 第17周：完成系统联调、功能测试、结题报告撰写与答辩准备。

## 六、小组分工
- 数据处理组：XXX，负责数据集收集、清洗、分词、预处理及批量生成；
- 模型训练组：XXX，负责模型搭建、训练、调参、验证及模型保存；
- 后端开发组：XXX，负责 FastAPI 接口开发、模型部署、接口测试；
- 前端设计组：XXX，负责 Gradio 界面设计、前后端联调、交互优化。